{
  "name": "demo-server",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/demo-server/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "commands": []
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "nx types server",
          "bun run --env-file apps/demo-server/.env --watch apps/demo-server/src/main.ts"
        ]
      }
    },
    "types": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "tsc --noEmit --skipLibCheck --showConfig -p apps/demo-server/tsconfig.lib.json"
        ]
      }
    },
    "compile": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "nx types server",
          "bun build --compile --minify --sourcemap ./apps/demo-server/index.ts --outfile server"
        ]
      }
    },
    "inspect": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "nx types server",
          "bun run --watch --inspect apps/server/index.ts"
        ]
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"]
    },
    "docker": {
      "dependsOn": ["build"],
      "command": "docker build -f apps/server/Dockerfile . -t server"
    }
  },
  "tags": []
}
