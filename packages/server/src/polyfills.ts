// import fetch, { RequestInfo, RequestInit, Response } from 'node-fetch';

// export interface GlobalWithFetch extends Global {
//   fetch: (url: RequestInfo, init?: RequestInit) => Promise<Response>;
// }

// declare const global: GlobalWithFetch;
// global.fetch = fetch;
